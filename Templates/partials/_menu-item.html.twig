{% extends 'base.html.twig' %}

{% block body %}
    <a
            href="{{ path(routeKey) }}"
            class="nav-item nav-link {{ getActualRouteName == routeKey ? 'active' : '' }}">
        {{ nameKey|trans }}
    </a>
{% endblock %}